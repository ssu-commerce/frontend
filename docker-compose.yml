services:
  # docs:
  #   container_name: docs
  #   build:
  #     context: .
  #     dockerfile: ./apps/backoffice/Dockerfile
  #   restart: always
  #   ports:
  #     - 3001:3001
  #   networks:
  #     - app_network
  commerce:
    container_name: commerce
    build:
      context: .
      dockerfile: ./apps/commerce/Dockerfile
    environment:
      NEXT_PUBLIC_API_KEY: ${{ variables.NEXT_PUBLIC_API_KEY }}
    restart: always
    ports:
      - 3000:3000
    networks:
      - app_network

# Define a network, which allows containers to communicate
# with each other, by using their container name as a hostname
networks:
  app_network:
    external: true
