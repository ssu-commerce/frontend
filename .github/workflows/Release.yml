name: Release

on:
  push:
    branches:
      - feature/docker-publish

jobs:
  build:
    name: Release
    runs-on: ubuntu-latest
    env:
      USERNAME: ${{ secrets.USERNAME }}
      IMAGE_NAME: ${{ secrets.IMAGE_NAME }}
    steps:
      - run: ls -al
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: "package.json"

      - name: Docker Login
        run: echo "${{ secrets.TOKEN }}" | docker login ghcr.io -u ${{ secrets.USERNAME }} --password-stdin

      - name: Docker Run
        run: |
          sudo docker compose down --rmi all
          sudo docker pull ghcr.io/ssu-commerce/frontend/commerce:latest
          sudo docker compose up -d

      - name: Docker Push
        run: docker push ghcr.io/ssu-commerce/frontend/commerce:latest